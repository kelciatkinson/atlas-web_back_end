#!/usr/bin/env python3
"""
Type-annotated function that takes two strings as arguments
and returns a concatenated string.
"""


def concat(str1: str, str2: str) -> str:
    """Returns the concatenation of two strings."""
    return str1 + str2
